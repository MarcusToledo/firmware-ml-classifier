# Quickstart: avaliação

Pré-requisitos: `008`, `009` e `010` implementadas e artefatos da `008` e
da `009` em `dataset/processed/`.

```bash
python scripts/evaluate.py
```

Abrir `reports/<timestamp>/report.md`. Para SC-002, rodar de novo e
comparar os CSV dos dois diretórios com `cmp`.
